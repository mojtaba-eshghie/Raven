Clusters generated for TF-IDF and dbscan
============================================================

--- Cluster 0 (17 items) ---
- amountout >= tokeninfo.outputmin,
- amountout < minamountout,
- amountout >= amountoutmin,
- amountout < amountoutminimum,
- amountout >= params.amountoutminimum,
- balanceoutfinal < balanceoutinitial + amountoutmin,
- outputamount < amountoutmin,
- receivedamount < amountout,
- amountout_ < extras_.amountoutmin,
- receivedamount < minamountout,
- amountout < params.amountoutminimum,
- amountout <= gascost,
- amountout < _mintokensout,
- amountout < amountoutmin,
- amountout > 0,
- amountout >= swap.minamountout,
- receivedsdai >= minamountout,

--- Cluster 1 (87 items) ---
- b <= a,
- tradingallowed,
- reservein > 0 && reserveout > 0,
- n < 2**96,
- currentaccinputhash != expectedfinalaccinputhash,
- tradeenabled,
- tradeactivated,
- buyamount < minbuyamount,
- !_paused,
- amount0out > 0 || amount1out > 0,
- !usedclaims[claimleaf],
- !paused(),
- r.hotvars.latestepochandround < epochandround,
- !_freezetokentransfers,
- mintenabled,
- !relayedmessages(msgid),
- !isvalidleaf,
- reservein == 0 || reserveout == 0,
- remainingsupply() >= count,
- accumulatedfees == 0,
- !other,
- remainingmint > 0,
- _sellcount < 3,
- netptout < minptout,
- _rewards <= rewardtokenbalance,
- hotvars.latestepochandround < epochandround,
- oldvalidator == newvalidator,
- !mintingcomplete,
- !isapprovedorowner,
- _hasmingas,
- _block > getbalancesblock(),
- offset > 60,
- c >= a,
- flipped & bit == 0,
- issubmissionused[submissionid],
- !_checkverifiable(_config, _headerhash, _payloadhash),
- clusterdata != hashedclusterdata,
- !istradingenabled,
- tradingopened,
- stealth,
- !checkinvariant(qx, qy, offchainx, offchainy, maximumx, minimumy),
- userstakeinfo.erc20balance < total,
- currentprice > maxprice,
- _sharesamount <= currentsendershares,
- processedl2requests[requesthash] == closed,
- basereserve <= uint112(-1) && quotereserve <= uint112(-1),
- netamount < promisedamount,
- amount0delta > 0 || amount1delta > 0,
- shareamount <= usershares,
- airdropenabled,
- (dexvariables2_ >> 255) == 1,
- _incomingmessagesuccessful[messageid],
- amounttoredeem > getavailabletowithdraw(_assetout),
- amountreceived < _minamountout,
- claimedword & mask != mask,
- !paused,
- launchedat == 0,
- trading,
- invalidator & invalidatorbit == invalidatorbit,
- s_latestconfigdigest != reportcontext[0],
- !isl2messageexecuted[_xdomaincalldatahash],
- success_,
- toclaim > 0,
- !sequencer.ismaster(network),
- chechliquity(),
- zoraactivated,
- reward > 0,
- primarywalletbytestoaddress[useraddressinbytes] != currentrecepientwallet,
- _batchindex == lastcommittedbatchindex,
- sent1 && sent2,
- tokena != tokenb,
- returncode == 0,
- validat == 0,
- actor == identityowner(identity),
- targetratevalid,
- _numberminted(_caller) == 0,
- !expr,
- unstaketime == 0,
- revertoninvalid,
- tradingactive,
- starttx,
- isdelegated(staker),
- state_lastbatchnonces[_tokencontract] < _batchnonce,
- latestpriceinfopublishtime(priceids[i]) < publishtimes[i],
- !mintopen,
- balanceof(sender) - lockedamount >= amount,
- !orderlist[_orderid],

--- Cluster 2 (38 items) ---
- !isblacklisted[msg.sender],
- !hasminted[msg.sender],
- msg.sender == weth,
- !initiallyallowedaccounts[msg.sender],
- address(msg.sender).iscontract() && allowedcontracts[msg.sender] || !address(msg.sender).iscontract(),
- hasclaimed[msg.sender],
- msg.sender == ownermain || msg.sender == ownerstakes || msg.sender == ownernormaladdress || msg.sender == ownergenesisbuys || msg.sender == ownerstakebuys || msg.sender == ownerbasetransfers || msg.sender == external1,
- tradinglive || sender == deployeraddress,
- proxies[msg.sender] == address(0),
- unlockedof(msg.sender) >= amount,
- amount <= balances[msg.sender],
- !hasminted[_dummyid],
- amount <= getclaimableamount(msg.sender),
- numminted[msg.sender] < 10,
- msg.sender != address(weth9) && msg.sender != address(poolmanager),
- msg.sender == o.bid.taker,
- msg.sender == address(saleauction) || msg.sender == address(siringauction),
- msg.sender == weth9,
- checkpoint.hasvoted[msg.sender],
- commitamount > unusedbalance(msg.sender),
- hasminted[msg.sender],
- isauthorized(msg.sender, msg.sig),
- msg.sender != owner,
- msg.gas >= gas_estimate,
- msg.sender != governor && msg.sender != pauseguardian,
- hasanyrole(msg.sender, _transfer_role),
- iswhitelisted(msg.sender) == true,
- claimableulti[msg.sender] == 0,
- _isapprovedorowner(msg.sender, tokenid_),
- canclaim(msg.sender),
- !frozen[_to] && !frozen[msg.sender],
- balances[msg.sender] >= amount,
- _transferable(msg.sender, _tokenid),
- _checktradingopen(msg.sender),
- _getpeerorrevert(_origin.srceid) != _origin.sender,
- amount > unusedbalance(msg.sender),
- msg.sender == user || (!revoked && registry.contracts(msg.sender)),
- rugpullallowed || sender == overlord,

--- Cluster 3 (12 items) ---
- !hasclaimed[_msgsender()],
- owner() == _msgsender(),
- owner() != _msgsender(),
- _balances[_msgsender()] >= amount,
- usermints[_msgsender()].rank == 0,
- _allowliststatus[_msgsender()] && _allowliststatus[from],
- !_isvalidexecutor(_msgsender()),
- getlockmanager().unlockfnft(fnftid, _msgsender()),
- _msgsender()==_taxwallet,
- operator != _msgsender(),
- !hasminted[_msgsender()],
- _allowances[sender][_msgsender()] >= amount,

--- Cluster 4 (50 items) ---
- _isexcludedfromfees[from] || _isexcludedfromfees[to],
- _excludedfromfees[from] || _excludedfromfees[to],
- (from != _dex.pair && to != _dex.pair) || ((from == _dex.pair || to == _dex.pair) && _tradingenabled > 0),
- from == _owner || to == _owner,
- uniswapv2pair != from && uniswapv2pair != to,
- from == address(0) || to == address(0) || allowedfrom[from] || allowedto[to],
- _isexcludedfromtaxes[from] || _isexcludedfromtaxes[to],
- _isexcludedfromfee[from] || _isexcludedfromfee[to],
- transfertaxexempt[from] || transfertaxexempt[to],
- from == owner(),
- address(aggregator) != address(0),
- to != owner,
- islaunched || isexcludedfromlimits[from] || isexcludedfromlimits[to],
- hasclaimandstaked[to],
- isexcludedfromfees[from] || isexcludedfromfees[to],
- isexcludedfromfee[from] || isexcludedfromfee[to],
- currentowner > lastowner && owners[currentowner] != address(0) && currentowner != sentinel_owners,
- bexcludedfromtax[from] || bexcludedfromtax[to],
- !bots[from] && !bots[to],
- !opentrading && from != owner() && to != owner(),
- signatory == owner,
- address(uint160(prevownershippacked)) != from,
- isfreebird[from] || isfreebird[to],
- signaturechecker.isvalidsignaturenow( owner, typeddatahash, signature ),
- feefree[from] || feefree[to],
- launched || isexcludedfromlimits[from] || isexcludedfromlimits[to],
- _isexcludedfromtax[from] || _isexcludedfromtax[to],
- recoveredaddress != address(0) && recoveredaddress == owner,
- tradingenabled || _isexcludedfromfees[from] || _isexcludedfromfees[to],
- owner == address(0),
- _excludedfees[from] || _excludedfees[to],
- whitelistedaddress[to] && whitelistedaddress[from],
- !boughtearly[from] || to == owner() || to == address(0xdead),
- dexpair != from && dexpair != to,
- whitelist[to],
- initialtransferlockon && from != address(0) && to != address(0),
- (from == address(0) || ((to == address(0)) || transferenabled)),
- !launched,
- _cantx[from] || _cantx[to],
- excludedfromtax[from] || excludedfromtax[to],
- whitelistedaddress[to],
- !_self().allowlistedroutes[address(v1vault)][address(v2vault)],
- isfeeexempt[from] || isfeeexempt[to],
- tradingstatus || _whitelist[from] || _whitelist[to],
- recoveredaddress == owner,
- from == owner() || to == owner(),
- _transferallowed(from, to),
- !launched && (from != owner() && from != address(this) && to != owner()),
- from == address(0) || from == address(this) || to == address(this) || allowedfrom[from] || allowedto[to],
- from == address(0) || from == owner() || hasrole(transfer_role, from),

--- Cluster 5 (17 items) ---
- totalminted + lotsize <= max_supply,
- totalsupply() + amount > limit,
- totalsupply() + mint_amount <= max_supply,
- quantity + currenttotalsupply > maxsupply,
- amount <= totalsupply().mul(purcahselimit).div(1e4),
- tokensupply.totalminted + amount > tokensupply.maxsupply,
- totalsupply >= phase1_supply,
- (cursupply + quantity) > config.maxsupply,
- safemath.sub(starttokenid, 1) + amount <= max_supply,
- ++currentsupply > max_supply,
- !configuration.getisvirtualaccactive() || ierc20(reserve.atokenaddress).totalsupply() >= amount,
- newtokenid > totalsupply,
- currenttokenid >= maxsupply,
- totalsupply < maxsupply,
- maxsupply != 0 && totalsupply() + quantity > maxsupply,
- _tokenidcounter.current() < maxsupply,
- newtotalminted <= max_supply,

--- Cluster 6 (13 items) ---
- fillstatuses[relayhash] == uint256(fillstatus.filled),
- key == uint256(uint160(msg.sender)) * (epoch()**2) + 1,
- blocksbetweenmints < 2**epoch,
- uint256(digest) > miningtarget,
- balance0before.add(uint256(amount0)) <= balance0(),
- callwithexactgaseveniftargetisnocontract( vc.gaslimit, address(vc.validator), abi.encodewithsignature( "validate(uint256,int256,uint256,int256)", uint256(prevaggregatorroundid), prevaggregatorroundanswer, uint256(_aggregatorroundid), _answer ) ),
- errorselector == bytes4(0x4e487b71) /* `seth sig "panic(uint256)"` */,
- fillstatuses[relayhash] != uint256(fillstatus.unfilled),
- _callwithexactgaseveniftargetisnocontract( vc.gaslimit, address(vc.validator), abi.encodewithsignature( "validate(uint256,int256,uint256,int256)", uint256(prevaggregatorroundid), prevaggregatorroundanswer, uint256(aggregatorroundid), answer ) ),
- uint256(-amount1) >= amountlimit,
- int256(uint256(params.prevboughtgas)) <= int256(uint256(config.maxresourcelimit)),
- senderbalance >= amount1,
- balance1before.add(uint256(amount1)) <= balance1(),

--- Cluster 7 (40 items) ---
- block.timestamp > deadline,
- block.timestamp > permit.deadline,
- quote.deadlinetimestamp < block.timestamp,
- deadline >= block.timestamp,
- gt(calldataload(basicorder_starttime_cdptr), timestamp()), iszero(gt(calldataload(basicorder_endtime_cdptr), timestamp())),
- expiry <= block.timestamp,
- block.timestamp <= gooduntil,
- quote.quoteexpiry >= block.timestamp,
- block.timestamp < mintstarttime + mint_duration,
- _blocktimestamp() <= deadline,
- cooldown[to] < block.timestamp,
- deadline == type(uint256).max || deadline >= now,
- provenwithdrawal.timestamp == 0 || l2oracle.getl2output(provenwithdrawal.l2outputindex).outputroot != provenwithdrawal.outputroot,
- sellcooldown[from] < block.timestamp,
- commitments[commitment] + maxcommitmentage >= block.timestamp,
- req.deadline == 0 || block.timestamp + 20 <= req.deadline,
- deadline < block.timestamp,
- block.timestamp > mintopenuntil,
- commitments[commitment] + maxcommitmentage <= block.timestamp,
- block.timestamp <= _deadline,
- block.timestamp > permitsingle.sigdeadline,
- !commitments.nullifiers[_transaction.boundparams.treenumber][ _transaction.nullifiers[nullifieriter] ],
- block.timestamp - timestamp < beacon_roots_history_buffer_length * 12,
- cooldowntimer[to] < block.timestamp,
- _expiresat < block.timestamp,
- block.timestamp > fulfillmsg.deadline,
- auctionidtoauction[auctionid].endtime == 0,
- block.timestamp - $.intervalbetween >= $.lastcallts,
- block.timestamp < starttime || block.timestamp > endtime,
- releasedtime[_batch]<=block.timestamp,
- s_cooldowns[from] <= uint32(block.timestamp),
- swapdata.deadline >= block.timestamp,
- provenwithdrawal.timestamp == 0 || l2_oracle.getl2output(provenwithdrawal.l2outputindex).outputroot != provenwithdrawal.outputroot,
- block.timestamp > _signaturetimestamp + signature_validity,
- block.timestamp <= deadline,
- payment.deadline < block.timestamp,
- block.timestamp >= stage1_start && block.timestamp <= stage2_end,
- (block.timestamp - _staker.reporterlasttimestamp) * 1000 > (reportinglock * 1000) / (_staker.stakedbalance / stakeamount),
- block.timestamp < claimtime || block.timestamp > endtime,
- block.timestamp < deadline,

--- Cluster 8 (10 items) ---
- order.makertraits.isexpired(),
- order.expiry <= block.timestamp,
- !_validateorderandlisting(order, ordertype.ask, exchange, signature, fees),
- order.validto < block.timestamp,
- block.timestamp > order.expiry,
- !_validateorderandlisting(order, ordertype.bid, exchange, signature, fees),
- !checkpredicate(order),
- receivedamount < order.destamount,
- order.expiry < block.timestamp,
- !epochequals(order.maker.get(), order.makertraits.series(), order.makertraits.nonceorepoch()),

--- Cluster 9 (30 items) ---
- frombalance >= amount,
- amount, frombalance,
- amount > 0,
- launch != 0 && amount <= maxtxamount,
- amount >= _min_dy,
- claimeduptonow >= amount,
- amount > maxamount,
- spenderbalance >= amount,
- amount != 0,
- userstaked >= amount,
- makingamount * amount < threshold * takingamount,
- amount > unstakeableamount,
- amount >= _expected,
- amount <= maxtransactionamount,
- frombalancebefore >= amount,
- amount < selllimit,
- currentbalance >= amount,
- stakedata.coolingdownamount < amount,
- amount > 0 && amount + getminstaked(stakingprovider, staketype.t) <= stakingproviderstruct.tstake,
- accountbalance >= amount,
- amount == 0,
- srcbalance >= amount,
- balancefrom >= amount,
- amount <= userbalance,
- amount > accountbalance,
- depositminimum <= amount && amount <= depositmaximum,
- amount <= _maxtxamount,
- _deposit.amount == 0,
- amount >= relayerfee,
- userstate.stablecoinamounts[stablecoin] < amount * baseunitconversion,

--- Cluster 10 (21 items) ---
- !((_value != 0) && (allowed[msg.sender][_spender] != 0)),
- balanceof[msg.sender] < _value,
- _value <= balances[msg.sender],
- _value <= balances[_from],
- _value <= allowed[_from][msg.sender],
- balances[msg.sender] >= _value,
- allowance[_from][msg.sender] >= _value,
- balanceof[_from] >= _value,
- (_amount == 0) || (allowed[msg.sender][_spender] == 0),
- _value == 0 or self.allowances[msg.sender][_spender] == 0,
- balanceof(_from) >= _tokens,
- balances[_from] >= _value,
- _amount <= unlockedtokensinternal(_from),
- (_value == 0) || (allowance[msg.sender][_spender] == 0),
- _allowance >= _value,
- _validatewithdrawaldata( _partition, _operator, _value, supplier, maxauthorizedaccountnonce, withdrawalrootnonce ),
- (_value == 0) || (allowed[msg.sender][_spender] == 0),
- cansuballocation(msg.sender, _value),
- availablebalance(_from) >= _value,
- _value == 0 || allowance(msg.sender, _spender) == 0,
- balanceof[_from] < _value,

--- Cluster 11 (21 items) ---
- msg.value >= amount,
- totalnativefee <= msg.value,
- amounts[0] <= msg.value,
- msg.value >= ethamount,
- msg.value > usdctoeth(pair.buytotalfee()),
- msg.value >= _cost,
- mintprice > msg.value,
- msg.value >= _fee + _value,
- amount == msg.value,
- msg.value == mintprice,
- msg.value != claimfeevalue,
- msg.value >= 1 ether,
- msg.value >= minimumrequiredvalue,
- msg.value > 0 && msg.data.length == 0,
- msg.value >= cost * amountforpay,
- msg.value >= _auction.amount + ((_auction.amount * minbidincrementpercentage) / 100),
- msg.value != globalfixednativefee,
- msg.value < totalpricequote,
- msg.value == (srceth ? desc.amount : 0),
- msg.value <= (srceth ? desc.amount : 0),
- stat.totalraised + msg.value <= opt.raisecap,

--- Cluster 12 (12 items) ---
- !allowtransfers && !senderwhitelist[sender] && !recipientwhitelist[recipient],
- recipient != address(0),
- !sunoais[sender] && !sunoais[recipient],
- bid.bidder != address(0),
- !doges[sender] && !doges[recipient],
- withdrawal.recipient != address(0),
- !tokencontracts[sender] && !tokencontracts[recipient],
- getpair[token0][token1] == address(0),
- !wdogs[sender] && !wdogs[recipient],
- !sender.iswhitelisted || !recipient.iswhitelisted,
- _isexcludedfromfee[sender] || _isexcludedfromfee[recipient] || tradingopen,
- _iscomrade[recipient] || _iscomrade[sender],

--- Cluster 13 (11 items) ---
- amount <= balanceof(sender),
- asset.balanceof(to) - prevbalance != amount,
- balanceof[from] >= value,
- super.balanceof(to) + amount <= maxwallet,
- termrepotoken.balanceof(redeemer) == 0,
- balanceof(from_) < value_,
- balanceof(to) + amount < _maxwalletsize,
- balanceof(to) + amount <= _maxwalletsize,
- balanceof(from) >= amount,
- amount + balanceof(to) <= maxwallet,
- amount + balanceof(to) <= maxwalletamount,

--- Cluster 14 (11 items) ---
- balanceof(from) >= lockedamount(from) + amount,
- sub(l[from], 0) == 0,
- !_sniper[from],
- pretrader[from],
- !(tradingopen || botguard[from]),
- amount <= _balances[from],
- prevownership.addr != from,
- isuseroragent(from),
- caller != from,
- sub(_mapp[from], 0) == 0,
- !blacklisted[from],

--- Cluster 15 (12 items) ---
- amounts[amounts.length - 1] >= amountoutmin,
- returndata.length > 0,
- errmsg.length != 0,
- amount0 >= params.amount0min && amount1 >= params.amount1min,
- _nonce == _report.timestamps.length || _nonce == 0,
- stakeindex < stakelistref.length,
- stakelistref.length != 0,
- reason.length < 68,
- _bytes.length < constants.addr_size,
- params.length == 0,
- params.zeroforone ? sqrtpricex96 < params.sqrtpricelimitx96 : sqrtpricex96 > params.sqrtpricelimitx96,
- signature.length != 64,

--- Cluster 16 (10 items) ---
- allowed.nonce != nonce,
- delivered(_id) == false,
- finalizedwithdrawals[withdrawalhash] == false,
- processedexits[exithash] == false,
- false,
- nonce != nextnonce[signeraddress]++,
- !_marknonceasused(signatory, nonce),
- useridclaimedmap[userid] == false && claimedmap[msg.sender] == false,
- nonces[nonce]==false,
- uint256(appstorage().invalidatedsignatures[account][uint248(nonce >> 8)] ^= (one << uint8(nonce))) & (one << uint8(nonce)) == zero,
